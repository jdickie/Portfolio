*If the image margins are not right, or the images are overflowing
 goto "swift theme options" and click save changes to regenerate the javascript file.

*Nav menus missing

Set them through

Appearance->menus

